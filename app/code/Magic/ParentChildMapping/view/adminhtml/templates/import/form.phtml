<?php
/** @var \Magic\ParentChildMapping\Block\Adminhtml\Import\Form $block */
?>
<div class="admin__data-grid-wrap">
    <form action="<?= $block->escapeUrl($block->getUploadUrl()) ?>"
          method="post"
          enctype="multipart/form-data"
          id="magic-parentchild-import-form">
        <input type="hidden" name="form_key" value="<?= $block->escapeHtml($block->getFormKey()) ?>"/>

        <fieldset class="admin__fieldset">
            <legend class="admin__legend">
                <span><?= $block->escapeHtml(__('Import Parent Child SKU Mappings')) ?></span>
            </legend>
            <br/>

            <div class="admin__field">
                <label class="admin__field-label" for="import_file">
                    <span><?= $block->escapeHtml(__('CSV File')) ?></span>
                    <span class="required">*</span>
                </label>
                <div class="admin__field-control">
                    <input type="file"
                           name="import_file"
                           id="import_file"
                           accept=".csv"
                           class="admin__control-file"
                           required/>
                    <p class="admin__field-note">
                        <?= $block->escapeHtml(__('Required columns: parent_sku, child_sku. Existing pairs will be skipped.')) ?>
                    </p>
                </div>
            </div>

            <div class="admin__field">
                <label class="admin__field-label">
                    <span><?= $block->escapeHtml(__('CSV Format Example')) ?></span>
                </label>
                <div class="admin__field-control">
                    <pre style="background:#f5f5f5;padding:10px;border:1px solid #ddd;display:inline-block;min-width:300px;">parent_sku,child_sku
SKU001,SKU001-A
SKU001,SKU001-B
SKU002,SKU002-A</pre>
                </div>
            </div>
        </fieldset>

        <div class="page-actions">
            <button type="button"
                    class="action-default scalable back"
                    onclick="location.href='<?= $block->escapeUrl($block->getBackUrl()) ?>'">
                <span><?= $block->escapeHtml(__('Back')) ?></span>
            </button>
            <button type="submit"
                    class="action-default scalable save primary"
                    id="import-submit-btn">
                <span><?= $block->escapeHtml(__('Import')) ?></span>
            </button>
        </div>
    </form>
</div>
